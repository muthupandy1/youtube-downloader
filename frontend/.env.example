# Copy this to .env.local and set your deployed backend URL, e.g.:
# VITE_API_BASE=https://youtube-downloader-backend.onrender.com/api
#
# If unset, the app falls back to http://localhost:8000/api for local dev.
VITE_API_BASE=
